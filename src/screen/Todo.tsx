import {Text, View} from 'react-native';

// 피그마 아트보드 10

const Todo = () => (
  <View
    style={{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    }}>
    <Text>Todo</Text>
  </View>
);

export default Todo;
